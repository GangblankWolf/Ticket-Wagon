import './CreateEvent.css';

function CreateEvent() {
   return(
      <div>
         This is the create event form where a signed in user can create a public event listing
      </div>
   )
}

export default CreateEvent;